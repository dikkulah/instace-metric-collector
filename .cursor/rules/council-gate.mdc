---
description: LLM Council meta gate — veto rules and phase boundaries
alwaysApply: true
---

# Council Gate

Before implementing, check V1–V20 vetoes in AGENTS.md and docs/VETO_REGISTRY.md.

Stop and ask the user if any veto would be violated.

**Current phase:** Go runtime active (ADR-009). **G5** = hub push client. **G7** = historic + alerts.

Phase constraints: OS logic in collectors only (V1), Docker optional (V4), payload contract stable (V6/V7), UI via snapshot store not logs (V16). Local CI: `make ci-fast` / `docs/LOCAL_CI.md`.
