---
description: Security and repo hygiene — secrets, logs, artifacts
globs: "**/.env*,.github/**,docker-compose.yml"
alwaysApply: false
---

# Security rules (V9–V10, V15)

## V9 — No committed credentials

Never commit API keys, tokens, or passwords in env files or compose overrides.

Use environment variables or external secret stores for production config.

## V10 — No secrets in logs

Do not log credentials, tokens, or full connection strings. Payload JSON must not include secrets.

## V15 — No build artifacts in git

Never commit:
- `go/bin/`, `go/internal/webui/dist/`, `go/web/frontend/node_modules/`
- `target/` (legacy Java)
- `metrics-collector.log*`, `smoke-*.log`
- `.DS_Store`

See `docs/REPO_HYGIENE.md`.
