---
description: MetricsPayload JSON contract — backward compatibility
globs: go/internal/payload/**,go/internal/logoutput/**,go/testdata/**
alwaysApply: false
---

# Payload rules (V6–V7)

## V6 — Backward compatibility

`MetricsPayload` field changes must be additive unless:
1. An ADR documents the breaking change
2. Version is bumped in release tags
3. Downstream consumers are notified

Prefer optional new fields with sensible defaults over renaming/removing fields.

## V7 — JSON log as contract

The `logoutput` JSON line per tick is an external integration contract.

- Document schema changes in `docs/DECISIONS/` ADR
- Update golden fixture in `go/testdata/` and smoke assertions when adding required fields
- Keep field names stable for log parsers

Current required fields: `cpuLoad`, `usedMemory`, `totalMemory`, `processInfos`, `serviceInfos`, `containers`, `diskUsage`, `networkUsage`.
